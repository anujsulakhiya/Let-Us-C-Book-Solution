#include<stdio.h>
int series(int,int,int);

int main()
{
  int fs,a,b;

  printf("Enter Any Two Positive Number to Find Its Fibonacci Sequence : ");
  scanf("%d%d",&a,&b);
  fs=series(a,b);
  printf("series = %d ",fs);

  return 0;
}
int series(int a,int b)
{
  
}
